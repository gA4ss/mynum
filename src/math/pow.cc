/* Raise to power
 * Returns base raised to the power exponent:
 *
 * base ^ exponent
 */
#include <mynum/math.h>

namespace mynum {
namespace math {

Float pow(const Float& base, const Float& exponent) {
  return mynum::pow(base, exponent);
}

} // namespace math
} // namespace mynum
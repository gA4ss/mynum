/* Compute cosine
 * Returns the cosine of an angle of x radians.
 */
#include <mynum/math.h>

namespace mynum {
namespace math {

Float cos(const Float& x) {
  return mynum::cos(x);
}

} // namespace math
} // namespace mynum
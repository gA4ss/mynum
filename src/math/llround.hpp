/* Returns the integer value that is nearest in value to x, with halfway 
 * cases rounded away from zero.
 *
 * The rounded value is returned as a value of type long long int. 
 * See lround for an equivalent function that returns a long int instead.
 */
template <class T>
T llrint(const T& x) {
  return x;
}